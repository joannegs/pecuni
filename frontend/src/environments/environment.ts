export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8081/api/v1',
  // Dev OAuth client (project "pecuni" on Google Cloud Console), authorized
  // JavaScript origin: http://localhost:4200. Only the client ID is needed —
  // the GIS ID-token flow never uses the client secret.
  googleClientId: '84775773852-8gng5suamrvb60sa5le63bu24n0h9rgc.apps.googleusercontent.com',
};
